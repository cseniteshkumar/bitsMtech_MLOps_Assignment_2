name: CD Pipeline - Build & Deploy

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/mlopsassignment2

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements_fastapi.txt
        pip install pytest pytest-cov

    - name: Run smoke tests
      run: bash tests/run_smoke_tests.sh

  build:
    runs-on: ubuntu-latest
    needs: smoke-tests
    outputs:
      tags: ${{ steps.meta.outputs.tags }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}

    - name: Extract Metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=sha,format=short
          type=raw,value=latest,enable=true

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        cache-from: type=gha
        cache-to: type=gha,mode=max


##### At present we are running the deployment on local machine, 
##### so the deployment step is commented out. To enable it, simply 
##### uncomment the following section and ensure your secrets are 
##### properly configured.

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #   - name: Deploy to Server via SSH
  #     uses: appleboy/ssh-action@v1.0.3
  #     with:
  #       host: ${{ secrets.DEPLOY_HOST }}
  #       username: ${{ secrets.DEPLOY_USER }}
  #       key: ${{ secrets.DEPLOY_KEY }}
  #       script: |
  #         # Login to Docker
  #         echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          
  #         # Pull the latest image
  #         docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          
  #         # Stop and remove existing container if it exists
  #         docker stop mlops-container || true
  #         docker rm mlops-container || true
          
  #         # Run the new container
  #         docker run -d \
  #           --name mlops-container \
  #           -p 80:80 \
  #           --restart unless-stopped \
  #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            
  #         # Cleanup old unused images
  #         docker image prune -f